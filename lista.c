#include <stdio.h>

// primeiramente, fazer uma area que aloque cada scanf em uma variavél

int main(){
    int a,b,c,d,e,f,g,h,i,j;

    scanf("%d %d %d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h, &i, &j);

    return 0;
}